services:
  - type: web
    name: apichallenge
    runtime: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /beneficiario
    envVars:
      - key: DB_URL
        sync: false
      - key: DB_USER
        sync: false
      - key: DB_PASSWORD
        sync: false
      - key: CORS_ORIGINS
        sync: false
